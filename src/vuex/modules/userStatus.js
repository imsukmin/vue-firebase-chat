// import * as types from '../mutation-types'

const state = {
  userStatus: {
    'isLogin': false,
    'username': 'guest',
    'userPic': ''
  }
}

/* eslint-disable no-shadow */
/* eslint-disable no-param-reassign */
/* eslint-disable no-plusplus */
const mutations = {
  // [types.SET_USERSTATUS] (state, dataFromServer) {
  //   state.userStatus = dataFromServer
  // }
}

export default {
  state,
  mutations
}
